/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ApprovalTarget {

    public static final ApprovalTarget NEW_WINDOW = new ApprovalTarget("new_window");
    public static final ApprovalTarget ANY = new ApprovalTarget("any");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ApprovalTarget> values = createValuesMap();
    private static final Map<String, ApprovalTargetEnum> enums = createEnumsMap();

    private final String value;

    private ApprovalTarget(String value) {
        this.value = value;
    }

    /**
     * Returns a ApprovalTarget with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ApprovalTarget
     */ 
    @JsonCreator
    public static ApprovalTarget of(String value) {
        synchronized (ApprovalTarget.class) {
            return values.computeIfAbsent(value, v -> new ApprovalTarget(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ApprovalTargetEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ApprovalTarget other = (ApprovalTarget) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ApprovalTarget [value=" + value + "]";
    }

    // return an array just like an enum
    public static ApprovalTarget[] values() {
        synchronized (ApprovalTarget.class) {
            return values.values().toArray(new ApprovalTarget[] {});
        }
    }

    private static final Map<String, ApprovalTarget> createValuesMap() {
        Map<String, ApprovalTarget> map = new LinkedHashMap<>();
        map.put("new_window", NEW_WINDOW);
        map.put("any", ANY);
        return map;
    }

    private static final Map<String, ApprovalTargetEnum> createEnumsMap() {
        Map<String, ApprovalTargetEnum> map = new HashMap<>();
        map.put("new_window", ApprovalTargetEnum.NEW_WINDOW);
        map.put("any", ApprovalTargetEnum.ANY);
        return map;
    }
    
    
    public enum ApprovalTargetEnum {

        NEW_WINDOW("new_window"),
        ANY("any"),;

        private final String value;

        private ApprovalTargetEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

