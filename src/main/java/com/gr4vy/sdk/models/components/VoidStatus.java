/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VoidStatus {

    public static final VoidStatus SUCCEEDED = new VoidStatus("succeeded");
    public static final VoidStatus PENDING = new VoidStatus("pending");
    public static final VoidStatus DECLINED = new VoidStatus("declined");
    public static final VoidStatus FAILED = new VoidStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VoidStatus> values = createValuesMap();
    private static final Map<String, VoidStatusEnum> enums = createEnumsMap();

    private final String value;

    private VoidStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a VoidStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VoidStatus
     */ 
    @JsonCreator
    public static VoidStatus of(String value) {
        synchronized (VoidStatus.class) {
            return values.computeIfAbsent(value, v -> new VoidStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VoidStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VoidStatus other = (VoidStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VoidStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static VoidStatus[] values() {
        synchronized (VoidStatus.class) {
            return values.values().toArray(new VoidStatus[] {});
        }
    }

    private static final Map<String, VoidStatus> createValuesMap() {
        Map<String, VoidStatus> map = new LinkedHashMap<>();
        map.put("succeeded", SUCCEEDED);
        map.put("pending", PENDING);
        map.put("declined", DECLINED);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, VoidStatusEnum> createEnumsMap() {
        Map<String, VoidStatusEnum> map = new HashMap<>();
        map.put("succeeded", VoidStatusEnum.SUCCEEDED);
        map.put("pending", VoidStatusEnum.PENDING);
        map.put("declined", VoidStatusEnum.DECLINED);
        map.put("failed", VoidStatusEnum.FAILED);
        return map;
    }
    
    
    public enum VoidStatusEnum {

        SUCCEEDED("succeeded"),
        PENDING("pending"),
        DECLINED("declined"),
        FAILED("failed"),;

        private final String value;

        private VoidStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

