/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ThreeDSecureMethod {

    public static final ThreeDSecureMethod CHALLENGE = new ThreeDSecureMethod("challenge");
    public static final ThreeDSecureMethod FRICTIONLESS = new ThreeDSecureMethod("frictionless");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ThreeDSecureMethod> values = createValuesMap();
    private static final Map<String, ThreeDSecureMethodEnum> enums = createEnumsMap();

    private final String value;

    private ThreeDSecureMethod(String value) {
        this.value = value;
    }

    /**
     * Returns a ThreeDSecureMethod with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ThreeDSecureMethod
     */ 
    @JsonCreator
    public static ThreeDSecureMethod of(String value) {
        synchronized (ThreeDSecureMethod.class) {
            return values.computeIfAbsent(value, v -> new ThreeDSecureMethod(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ThreeDSecureMethodEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ThreeDSecureMethod other = (ThreeDSecureMethod) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ThreeDSecureMethod [value=" + value + "]";
    }

    // return an array just like an enum
    public static ThreeDSecureMethod[] values() {
        synchronized (ThreeDSecureMethod.class) {
            return values.values().toArray(new ThreeDSecureMethod[] {});
        }
    }

    private static final Map<String, ThreeDSecureMethod> createValuesMap() {
        Map<String, ThreeDSecureMethod> map = new LinkedHashMap<>();
        map.put("challenge", CHALLENGE);
        map.put("frictionless", FRICTIONLESS);
        return map;
    }

    private static final Map<String, ThreeDSecureMethodEnum> createEnumsMap() {
        Map<String, ThreeDSecureMethodEnum> map = new HashMap<>();
        map.put("challenge", ThreeDSecureMethodEnum.CHALLENGE);
        map.put("frictionless", ThreeDSecureMethodEnum.FRICTIONLESS);
        return map;
    }
    
    
    public enum ThreeDSecureMethodEnum {

        CHALLENGE("challenge"),
        FRICTIONLESS("frictionless"),;

        private final String value;

        private ThreeDSecureMethodEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

