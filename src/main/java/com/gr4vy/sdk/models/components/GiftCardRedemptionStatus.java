/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class GiftCardRedemptionStatus {

    public static final GiftCardRedemptionStatus CREATED = new GiftCardRedemptionStatus("created");
    public static final GiftCardRedemptionStatus SUCCEEDED = new GiftCardRedemptionStatus("succeeded");
    public static final GiftCardRedemptionStatus FAILED = new GiftCardRedemptionStatus("failed");
    public static final GiftCardRedemptionStatus SKIPPED = new GiftCardRedemptionStatus("skipped");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, GiftCardRedemptionStatus> values = createValuesMap();
    private static final Map<String, GiftCardRedemptionStatusEnum> enums = createEnumsMap();

    private final String value;

    private GiftCardRedemptionStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a GiftCardRedemptionStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as GiftCardRedemptionStatus
     */ 
    @JsonCreator
    public static GiftCardRedemptionStatus of(String value) {
        synchronized (GiftCardRedemptionStatus.class) {
            return values.computeIfAbsent(value, v -> new GiftCardRedemptionStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<GiftCardRedemptionStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        GiftCardRedemptionStatus other = (GiftCardRedemptionStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "GiftCardRedemptionStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static GiftCardRedemptionStatus[] values() {
        synchronized (GiftCardRedemptionStatus.class) {
            return values.values().toArray(new GiftCardRedemptionStatus[] {});
        }
    }

    private static final Map<String, GiftCardRedemptionStatus> createValuesMap() {
        Map<String, GiftCardRedemptionStatus> map = new LinkedHashMap<>();
        map.put("created", CREATED);
        map.put("succeeded", SUCCEEDED);
        map.put("failed", FAILED);
        map.put("skipped", SKIPPED);
        return map;
    }

    private static final Map<String, GiftCardRedemptionStatusEnum> createEnumsMap() {
        Map<String, GiftCardRedemptionStatusEnum> map = new HashMap<>();
        map.put("created", GiftCardRedemptionStatusEnum.CREATED);
        map.put("succeeded", GiftCardRedemptionStatusEnum.SUCCEEDED);
        map.put("failed", GiftCardRedemptionStatusEnum.FAILED);
        map.put("skipped", GiftCardRedemptionStatusEnum.SKIPPED);
        return map;
    }
    
    
    public enum GiftCardRedemptionStatusEnum {

        CREATED("created"),
        SUCCEEDED("succeeded"),
        FAILED("failed"),
        SKIPPED("skipped"),;

        private final String value;

        private GiftCardRedemptionStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

