/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Mode {

    public static final Mode CARD = new Mode("card");
    public static final Mode REDIRECT = new Mode("redirect");
    public static final Mode APPLEPAY = new Mode("applepay");
    public static final Mode GOOGLEPAY = new Mode("googlepay");
    public static final Mode CHECKOUT_SESSION = new Mode("checkout-session");
    public static final Mode CLICK_TO_PAY = new Mode("click-to-pay");
    public static final Mode GIFT_CARD = new Mode("gift-card");
    public static final Mode BANK = new Mode("bank");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Mode> values = createValuesMap();
    private static final Map<String, ModeEnum> enums = createEnumsMap();

    private final String value;

    private Mode(String value) {
        this.value = value;
    }

    /**
     * Returns a Mode with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Mode
     */ 
    @JsonCreator
    public static Mode of(String value) {
        synchronized (Mode.class) {
            return values.computeIfAbsent(value, v -> new Mode(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ModeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Mode other = (Mode) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Mode [value=" + value + "]";
    }

    // return an array just like an enum
    public static Mode[] values() {
        synchronized (Mode.class) {
            return values.values().toArray(new Mode[] {});
        }
    }

    private static final Map<String, Mode> createValuesMap() {
        Map<String, Mode> map = new LinkedHashMap<>();
        map.put("card", CARD);
        map.put("redirect", REDIRECT);
        map.put("applepay", APPLEPAY);
        map.put("googlepay", GOOGLEPAY);
        map.put("checkout-session", CHECKOUT_SESSION);
        map.put("click-to-pay", CLICK_TO_PAY);
        map.put("gift-card", GIFT_CARD);
        map.put("bank", BANK);
        return map;
    }

    private static final Map<String, ModeEnum> createEnumsMap() {
        Map<String, ModeEnum> map = new HashMap<>();
        map.put("card", ModeEnum.CARD);
        map.put("redirect", ModeEnum.REDIRECT);
        map.put("applepay", ModeEnum.APPLEPAY);
        map.put("googlepay", ModeEnum.GOOGLEPAY);
        map.put("checkout-session", ModeEnum.CHECKOUT_SESSION);
        map.put("click-to-pay", ModeEnum.CLICK_TO_PAY);
        map.put("gift-card", ModeEnum.GIFT_CARD);
        map.put("bank", ModeEnum.BANK);
        return map;
    }
    
    
    public enum ModeEnum {

        CARD("card"),
        REDIRECT("redirect"),
        APPLEPAY("applepay"),
        GOOGLEPAY("googlepay"),
        CHECKOUT_SESSION("checkout-session"),
        CLICK_TO_PAY("click-to-pay"),
        GIFT_CARD("gift-card"),
        BANK("bank"),;

        private final String value;

        private ModeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

