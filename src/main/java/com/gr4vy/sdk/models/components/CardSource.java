/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CardSource {

    public static final CardSource APPLE_PAY = new CardSource("apple-pay");
    public static final CardSource GOOGLE_PAY = new CardSource("google-pay");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CardSource> values = createValuesMap();
    private static final Map<String, CardSourceEnum> enums = createEnumsMap();

    private final String value;

    private CardSource(String value) {
        this.value = value;
    }

    /**
     * Returns a CardSource with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CardSource
     */ 
    @JsonCreator
    public static CardSource of(String value) {
        synchronized (CardSource.class) {
            return values.computeIfAbsent(value, v -> new CardSource(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CardSourceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CardSource other = (CardSource) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CardSource [value=" + value + "]";
    }

    // return an array just like an enum
    public static CardSource[] values() {
        synchronized (CardSource.class) {
            return values.values().toArray(new CardSource[] {});
        }
    }

    private static final Map<String, CardSource> createValuesMap() {
        Map<String, CardSource> map = new LinkedHashMap<>();
        map.put("apple-pay", APPLE_PAY);
        map.put("google-pay", GOOGLE_PAY);
        return map;
    }

    private static final Map<String, CardSourceEnum> createEnumsMap() {
        Map<String, CardSourceEnum> map = new HashMap<>();
        map.put("apple-pay", CardSourceEnum.APPLE_PAY);
        map.put("google-pay", CardSourceEnum.GOOGLE_PAY);
        return map;
    }
    
    
    public enum CardSourceEnum {

        APPLE_PAY("apple-pay"),
        GOOGLE_PAY("google-pay"),;

        private final String value;

        private CardSourceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

