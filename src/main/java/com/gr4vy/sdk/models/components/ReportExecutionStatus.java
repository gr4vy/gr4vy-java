/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ReportExecutionStatus {

    public static final ReportExecutionStatus DISPATCHED = new ReportExecutionStatus("dispatched");
    public static final ReportExecutionStatus FAILED = new ReportExecutionStatus("failed");
    public static final ReportExecutionStatus PENDING = new ReportExecutionStatus("pending");
    public static final ReportExecutionStatus PROCESSING = new ReportExecutionStatus("processing");
    public static final ReportExecutionStatus SUCCEEDED = new ReportExecutionStatus("succeeded");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ReportExecutionStatus> values = createValuesMap();
    private static final Map<String, ReportExecutionStatusEnum> enums = createEnumsMap();

    private final String value;

    private ReportExecutionStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ReportExecutionStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ReportExecutionStatus
     */ 
    @JsonCreator
    public static ReportExecutionStatus of(String value) {
        synchronized (ReportExecutionStatus.class) {
            return values.computeIfAbsent(value, v -> new ReportExecutionStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ReportExecutionStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ReportExecutionStatus other = (ReportExecutionStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ReportExecutionStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ReportExecutionStatus[] values() {
        synchronized (ReportExecutionStatus.class) {
            return values.values().toArray(new ReportExecutionStatus[] {});
        }
    }

    private static final Map<String, ReportExecutionStatus> createValuesMap() {
        Map<String, ReportExecutionStatus> map = new LinkedHashMap<>();
        map.put("dispatched", DISPATCHED);
        map.put("failed", FAILED);
        map.put("pending", PENDING);
        map.put("processing", PROCESSING);
        map.put("succeeded", SUCCEEDED);
        return map;
    }

    private static final Map<String, ReportExecutionStatusEnum> createEnumsMap() {
        Map<String, ReportExecutionStatusEnum> map = new HashMap<>();
        map.put("dispatched", ReportExecutionStatusEnum.DISPATCHED);
        map.put("failed", ReportExecutionStatusEnum.FAILED);
        map.put("pending", ReportExecutionStatusEnum.PENDING);
        map.put("processing", ReportExecutionStatusEnum.PROCESSING);
        map.put("succeeded", ReportExecutionStatusEnum.SUCCEEDED);
        return map;
    }
    
    
    public enum ReportExecutionStatusEnum {

        DISPATCHED("dispatched"),
        FAILED("failed"),
        PENDING("pending"),
        PROCESSING("processing"),
        SUCCEEDED("succeeded"),;

        private final String value;

        private ReportExecutionStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

