/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * Source
 * 
 * <p>The platform that the Paze session is being created for. Defaults to `web`.
 */
public class Source {

    public static final Source WEB = new Source("web");
    public static final Source MOBILE = new Source("mobile");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Source> values = createValuesMap();
    private static final Map<String, SourceEnum> enums = createEnumsMap();

    private final String value;

    private Source(String value) {
        this.value = value;
    }

    /**
     * Returns a Source with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Source
     */ 
    @JsonCreator
    public static Source of(String value) {
        synchronized (Source.class) {
            return values.computeIfAbsent(value, v -> new Source(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<SourceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Source other = (Source) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Source [value=" + value + "]";
    }

    // return an array just like an enum
    public static Source[] values() {
        synchronized (Source.class) {
            return values.values().toArray(new Source[] {});
        }
    }

    private static final Map<String, Source> createValuesMap() {
        Map<String, Source> map = new LinkedHashMap<>();
        map.put("web", WEB);
        map.put("mobile", MOBILE);
        return map;
    }

    private static final Map<String, SourceEnum> createEnumsMap() {
        Map<String, SourceEnum> map = new HashMap<>();
        map.put("web", SourceEnum.WEB);
        map.put("mobile", SourceEnum.MOBILE);
        return map;
    }
    
    
    public enum SourceEnum {

        WEB("web"),
        MOBILE("mobile"),;

        private final String value;

        private SourceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

