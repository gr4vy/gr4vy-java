/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class IntegrationClient {

    public static final IntegrationClient REDIRECT = new IntegrationClient("redirect");
    public static final IntegrationClient WEB = new IntegrationClient("web");
    public static final IntegrationClient ANDROID = new IntegrationClient("android");
    public static final IntegrationClient IOS = new IntegrationClient("ios");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, IntegrationClient> values = createValuesMap();
    private static final Map<String, IntegrationClientEnum> enums = createEnumsMap();

    private final String value;

    private IntegrationClient(String value) {
        this.value = value;
    }

    /**
     * Returns a IntegrationClient with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as IntegrationClient
     */ 
    @JsonCreator
    public static IntegrationClient of(String value) {
        synchronized (IntegrationClient.class) {
            return values.computeIfAbsent(value, v -> new IntegrationClient(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<IntegrationClientEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        IntegrationClient other = (IntegrationClient) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "IntegrationClient [value=" + value + "]";
    }

    // return an array just like an enum
    public static IntegrationClient[] values() {
        synchronized (IntegrationClient.class) {
            return values.values().toArray(new IntegrationClient[] {});
        }
    }

    private static final Map<String, IntegrationClient> createValuesMap() {
        Map<String, IntegrationClient> map = new LinkedHashMap<>();
        map.put("redirect", REDIRECT);
        map.put("web", WEB);
        map.put("android", ANDROID);
        map.put("ios", IOS);
        return map;
    }

    private static final Map<String, IntegrationClientEnum> createEnumsMap() {
        Map<String, IntegrationClientEnum> map = new HashMap<>();
        map.put("redirect", IntegrationClientEnum.REDIRECT);
        map.put("web", IntegrationClientEnum.WEB);
        map.put("android", IntegrationClientEnum.ANDROID);
        map.put("ios", IntegrationClientEnum.IOS);
        return map;
    }
    
    
    public enum IntegrationClientEnum {

        REDIRECT("redirect"),
        WEB("web"),
        ANDROID("android"),
        IOS("ios"),;

        private final String value;

        private IntegrationClientEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

