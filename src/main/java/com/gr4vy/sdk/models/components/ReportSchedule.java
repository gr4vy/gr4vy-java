/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ReportSchedule {

    public static final ReportSchedule DAILY = new ReportSchedule("daily");
    public static final ReportSchedule MONTHLY = new ReportSchedule("monthly");
    public static final ReportSchedule ONCE = new ReportSchedule("once");
    public static final ReportSchedule WEEKLY = new ReportSchedule("weekly");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ReportSchedule> values = createValuesMap();
    private static final Map<String, ReportScheduleEnum> enums = createEnumsMap();

    private final String value;

    private ReportSchedule(String value) {
        this.value = value;
    }

    /**
     * Returns a ReportSchedule with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ReportSchedule
     */ 
    @JsonCreator
    public static ReportSchedule of(String value) {
        synchronized (ReportSchedule.class) {
            return values.computeIfAbsent(value, v -> new ReportSchedule(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ReportScheduleEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ReportSchedule other = (ReportSchedule) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ReportSchedule [value=" + value + "]";
    }

    // return an array just like an enum
    public static ReportSchedule[] values() {
        synchronized (ReportSchedule.class) {
            return values.values().toArray(new ReportSchedule[] {});
        }
    }

    private static final Map<String, ReportSchedule> createValuesMap() {
        Map<String, ReportSchedule> map = new LinkedHashMap<>();
        map.put("daily", DAILY);
        map.put("monthly", MONTHLY);
        map.put("once", ONCE);
        map.put("weekly", WEEKLY);
        return map;
    }

    private static final Map<String, ReportScheduleEnum> createEnumsMap() {
        Map<String, ReportScheduleEnum> map = new HashMap<>();
        map.put("daily", ReportScheduleEnum.DAILY);
        map.put("monthly", ReportScheduleEnum.MONTHLY);
        map.put("once", ReportScheduleEnum.ONCE);
        map.put("weekly", ReportScheduleEnum.WEEKLY);
        return map;
    }
    
    
    public enum ReportScheduleEnum {

        DAILY("daily"),
        MONTHLY("monthly"),
        ONCE("once"),
        WEEKLY("weekly"),;

        private final String value;

        private ReportScheduleEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

