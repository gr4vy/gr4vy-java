/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ReportSpecModel {

    public static final ReportSpecModel TRANSACTIONS = new ReportSpecModel("transactions");
    public static final ReportSpecModel TRANSACTION_RETRIES = new ReportSpecModel("transaction_retries");
    public static final ReportSpecModel DETAILED_SETTLEMENT = new ReportSpecModel("detailed_settlement");
    public static final ReportSpecModel ACCOUNTS_RECEIVABLES = new ReportSpecModel("accounts_receivables");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ReportSpecModel> values = createValuesMap();
    private static final Map<String, ReportSpecModelEnum> enums = createEnumsMap();

    private final String value;

    private ReportSpecModel(String value) {
        this.value = value;
    }

    /**
     * Returns a ReportSpecModel with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ReportSpecModel
     */ 
    @JsonCreator
    public static ReportSpecModel of(String value) {
        synchronized (ReportSpecModel.class) {
            return values.computeIfAbsent(value, v -> new ReportSpecModel(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ReportSpecModelEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ReportSpecModel other = (ReportSpecModel) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ReportSpecModel [value=" + value + "]";
    }

    // return an array just like an enum
    public static ReportSpecModel[] values() {
        synchronized (ReportSpecModel.class) {
            return values.values().toArray(new ReportSpecModel[] {});
        }
    }

    private static final Map<String, ReportSpecModel> createValuesMap() {
        Map<String, ReportSpecModel> map = new LinkedHashMap<>();
        map.put("transactions", TRANSACTIONS);
        map.put("transaction_retries", TRANSACTION_RETRIES);
        map.put("detailed_settlement", DETAILED_SETTLEMENT);
        map.put("accounts_receivables", ACCOUNTS_RECEIVABLES);
        return map;
    }

    private static final Map<String, ReportSpecModelEnum> createEnumsMap() {
        Map<String, ReportSpecModelEnum> map = new HashMap<>();
        map.put("transactions", ReportSpecModelEnum.TRANSACTIONS);
        map.put("transaction_retries", ReportSpecModelEnum.TRANSACTION_RETRIES);
        map.put("detailed_settlement", ReportSpecModelEnum.DETAILED_SETTLEMENT);
        map.put("accounts_receivables", ReportSpecModelEnum.ACCOUNTS_RECEIVABLES);
        return map;
    }
    
    
    public enum ReportSpecModelEnum {

        TRANSACTIONS("transactions"),
        TRANSACTION_RETRIES("transaction_retries"),
        DETAILED_SETTLEMENT("detailed_settlement"),
        ACCOUNTS_RECEIVABLES("accounts_receivables"),;

        private final String value;

        private ReportSpecModelEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

