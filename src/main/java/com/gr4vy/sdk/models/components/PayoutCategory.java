/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class PayoutCategory {

    public static final PayoutCategory ONLINE_GAMBLING = new PayoutCategory("online_gambling");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, PayoutCategory> values = createValuesMap();
    private static final Map<String, PayoutCategoryEnum> enums = createEnumsMap();

    private final String value;

    private PayoutCategory(String value) {
        this.value = value;
    }

    /**
     * Returns a PayoutCategory with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as PayoutCategory
     */ 
    @JsonCreator
    public static PayoutCategory of(String value) {
        synchronized (PayoutCategory.class) {
            return values.computeIfAbsent(value, v -> new PayoutCategory(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<PayoutCategoryEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        PayoutCategory other = (PayoutCategory) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "PayoutCategory [value=" + value + "]";
    }

    // return an array just like an enum
    public static PayoutCategory[] values() {
        synchronized (PayoutCategory.class) {
            return values.values().toArray(new PayoutCategory[] {});
        }
    }

    private static final Map<String, PayoutCategory> createValuesMap() {
        Map<String, PayoutCategory> map = new LinkedHashMap<>();
        map.put("online_gambling", ONLINE_GAMBLING);
        return map;
    }

    private static final Map<String, PayoutCategoryEnum> createEnumsMap() {
        Map<String, PayoutCategoryEnum> map = new HashMap<>();
        map.put("online_gambling", PayoutCategoryEnum.ONLINE_GAMBLING);
        return map;
    }
    
    
    public enum PayoutCategoryEnum {

        ONLINE_GAMBLING("online_gambling"),;

        private final String value;

        private PayoutCategoryEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

