/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class ThreeDSecureStatus {

    public static final ThreeDSecureStatus SETUP_ERROR = new ThreeDSecureStatus("setup_error");
    public static final ThreeDSecureStatus ERROR = new ThreeDSecureStatus("error");
    public static final ThreeDSecureStatus DECLINED = new ThreeDSecureStatus("declined");
    public static final ThreeDSecureStatus CANCELLED = new ThreeDSecureStatus("cancelled");
    public static final ThreeDSecureStatus COMPLETE = new ThreeDSecureStatus("complete");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, ThreeDSecureStatus> values = createValuesMap();
    private static final Map<String, ThreeDSecureStatusEnum> enums = createEnumsMap();

    private final String value;

    private ThreeDSecureStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a ThreeDSecureStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as ThreeDSecureStatus
     */ 
    @JsonCreator
    public static ThreeDSecureStatus of(String value) {
        synchronized (ThreeDSecureStatus.class) {
            return values.computeIfAbsent(value, v -> new ThreeDSecureStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<ThreeDSecureStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        ThreeDSecureStatus other = (ThreeDSecureStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "ThreeDSecureStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static ThreeDSecureStatus[] values() {
        synchronized (ThreeDSecureStatus.class) {
            return values.values().toArray(new ThreeDSecureStatus[] {});
        }
    }

    private static final Map<String, ThreeDSecureStatus> createValuesMap() {
        Map<String, ThreeDSecureStatus> map = new LinkedHashMap<>();
        map.put("setup_error", SETUP_ERROR);
        map.put("error", ERROR);
        map.put("declined", DECLINED);
        map.put("cancelled", CANCELLED);
        map.put("complete", COMPLETE);
        return map;
    }

    private static final Map<String, ThreeDSecureStatusEnum> createEnumsMap() {
        Map<String, ThreeDSecureStatusEnum> map = new HashMap<>();
        map.put("setup_error", ThreeDSecureStatusEnum.SETUP_ERROR);
        map.put("error", ThreeDSecureStatusEnum.ERROR);
        map.put("declined", ThreeDSecureStatusEnum.DECLINED);
        map.put("cancelled", ThreeDSecureStatusEnum.CANCELLED);
        map.put("complete", ThreeDSecureStatusEnum.COMPLETE);
        return map;
    }
    
    
    public enum ThreeDSecureStatusEnum {

        SETUP_ERROR("setup_error"),
        ERROR("error"),
        DECLINED("declined"),
        CANCELLED("cancelled"),
        COMPLETE("complete"),;

        private final String value;

        private ThreeDSecureStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

