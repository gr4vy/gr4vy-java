/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * UserDevice
 * 
 * <p>The platform that is being used to access the website.
 */
public class UserDevice {

    public static final UserDevice DESKTOP = new UserDevice("desktop");
    public static final UserDevice MOBILE = new UserDevice("mobile");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, UserDevice> values = createValuesMap();
    private static final Map<String, UserDeviceEnum> enums = createEnumsMap();

    private final String value;

    private UserDevice(String value) {
        this.value = value;
    }

    /**
     * Returns a UserDevice with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as UserDevice
     */ 
    @JsonCreator
    public static UserDevice of(String value) {
        synchronized (UserDevice.class) {
            return values.computeIfAbsent(value, v -> new UserDevice(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<UserDeviceEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        UserDevice other = (UserDevice) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "UserDevice [value=" + value + "]";
    }

    // return an array just like an enum
    public static UserDevice[] values() {
        synchronized (UserDevice.class) {
            return values.values().toArray(new UserDevice[] {});
        }
    }

    private static final Map<String, UserDevice> createValuesMap() {
        Map<String, UserDevice> map = new LinkedHashMap<>();
        map.put("desktop", DESKTOP);
        map.put("mobile", MOBILE);
        return map;
    }

    private static final Map<String, UserDeviceEnum> createEnumsMap() {
        Map<String, UserDeviceEnum> map = new HashMap<>();
        map.put("desktop", UserDeviceEnum.DESKTOP);
        map.put("mobile", UserDeviceEnum.MOBILE);
        return map;
    }
    
    
    public enum UserDeviceEnum {

        DESKTOP("desktop"),
        MOBILE("mobile"),;

        private final String value;

        private UserDeviceEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

