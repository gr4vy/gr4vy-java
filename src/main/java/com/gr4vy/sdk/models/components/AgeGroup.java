/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class AgeGroup {

    public static final AgeGroup ADULT = new AgeGroup("adult");
    public static final AgeGroup INFANT = new AgeGroup("infant");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, AgeGroup> values = createValuesMap();
    private static final Map<String, AgeGroupEnum> enums = createEnumsMap();

    private final String value;

    private AgeGroup(String value) {
        this.value = value;
    }

    /**
     * Returns a AgeGroup with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as AgeGroup
     */ 
    @JsonCreator
    public static AgeGroup of(String value) {
        synchronized (AgeGroup.class) {
            return values.computeIfAbsent(value, v -> new AgeGroup(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<AgeGroupEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        AgeGroup other = (AgeGroup) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "AgeGroup [value=" + value + "]";
    }

    // return an array just like an enum
    public static AgeGroup[] values() {
        synchronized (AgeGroup.class) {
            return values.values().toArray(new AgeGroup[] {});
        }
    }

    private static final Map<String, AgeGroup> createValuesMap() {
        Map<String, AgeGroup> map = new LinkedHashMap<>();
        map.put("adult", ADULT);
        map.put("infant", INFANT);
        return map;
    }

    private static final Map<String, AgeGroupEnum> createEnumsMap() {
        Map<String, AgeGroupEnum> map = new HashMap<>();
        map.put("adult", AgeGroupEnum.ADULT);
        map.put("infant", AgeGroupEnum.INFANT);
        return map;
    }
    
    
    public enum AgeGroupEnum {

        ADULT("adult"),
        INFANT("infant"),;

        private final String value;

        private AgeGroupEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

