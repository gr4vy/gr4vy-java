/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CancelStatus {

    public static final CancelStatus SUCCEEDED = new CancelStatus("succeeded");
    public static final CancelStatus PENDING = new CancelStatus("pending");
    public static final CancelStatus FAILED = new CancelStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CancelStatus> values = createValuesMap();
    private static final Map<String, CancelStatusEnum> enums = createEnumsMap();

    private final String value;

    private CancelStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a CancelStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CancelStatus
     */ 
    @JsonCreator
    public static CancelStatus of(String value) {
        synchronized (CancelStatus.class) {
            return values.computeIfAbsent(value, v -> new CancelStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CancelStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CancelStatus other = (CancelStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CancelStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static CancelStatus[] values() {
        synchronized (CancelStatus.class) {
            return values.values().toArray(new CancelStatus[] {});
        }
    }

    private static final Map<String, CancelStatus> createValuesMap() {
        Map<String, CancelStatus> map = new LinkedHashMap<>();
        map.put("succeeded", SUCCEEDED);
        map.put("pending", PENDING);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, CancelStatusEnum> createEnumsMap() {
        Map<String, CancelStatusEnum> map = new HashMap<>();
        map.put("succeeded", CancelStatusEnum.SUCCEEDED);
        map.put("pending", CancelStatusEnum.PENDING);
        map.put("failed", CancelStatusEnum.FAILED);
        return map;
    }
    
    
    public enum CancelStatusEnum {

        SUCCEEDED("succeeded"),
        PENDING("pending"),
        FAILED("failed"),;

        private final String value;

        private CancelStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

