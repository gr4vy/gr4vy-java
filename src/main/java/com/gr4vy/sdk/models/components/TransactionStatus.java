/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = TransactionStatus._Deserializer.class)
@JsonSerialize(using = TransactionStatus._Serializer.class)
public class TransactionStatus {

    public static final TransactionStatus PROCESSING = new TransactionStatus("processing");
    public static final TransactionStatus AUTHORIZATION_SUCCEEDED = new TransactionStatus("authorization_succeeded");
    public static final TransactionStatus AUTHORIZATION_DECLINED = new TransactionStatus("authorization_declined");
    public static final TransactionStatus AUTHORIZATION_FAILED = new TransactionStatus("authorization_failed");
    public static final TransactionStatus AUTHORIZATION_VOIDED = new TransactionStatus("authorization_voided");
    public static final TransactionStatus AUTHORIZATION_VOID_PENDING = new TransactionStatus("authorization_void_pending");
    public static final TransactionStatus CAPTURE_SUCCEEDED = new TransactionStatus("capture_succeeded");
    public static final TransactionStatus CAPTURE_PENDING = new TransactionStatus("capture_pending");
    public static final TransactionStatus BUYER_APPROVAL_PENDING = new TransactionStatus("buyer_approval_pending");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TransactionStatus> values = createValuesMap();
    private static final Map<String, TransactionStatusEnum> enums = createEnumsMap();

    private final String value;

    private TransactionStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a TransactionStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TransactionStatus
     */ 
    public static TransactionStatus of(String value) {
        synchronized (TransactionStatus.class) {
            return values.computeIfAbsent(value, v -> new TransactionStatus(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<TransactionStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TransactionStatus other = (TransactionStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TransactionStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static TransactionStatus[] values() {
        synchronized (TransactionStatus.class) {
            return values.values().toArray(new TransactionStatus[] {});
        }
    }

    private static final Map<String, TransactionStatus> createValuesMap() {
        Map<String, TransactionStatus> map = new LinkedHashMap<>();
        map.put("processing", PROCESSING);
        map.put("authorization_succeeded", AUTHORIZATION_SUCCEEDED);
        map.put("authorization_declined", AUTHORIZATION_DECLINED);
        map.put("authorization_failed", AUTHORIZATION_FAILED);
        map.put("authorization_voided", AUTHORIZATION_VOIDED);
        map.put("authorization_void_pending", AUTHORIZATION_VOID_PENDING);
        map.put("capture_succeeded", CAPTURE_SUCCEEDED);
        map.put("capture_pending", CAPTURE_PENDING);
        map.put("buyer_approval_pending", BUYER_APPROVAL_PENDING);
        return map;
    }

    private static final Map<String, TransactionStatusEnum> createEnumsMap() {
        Map<String, TransactionStatusEnum> map = new HashMap<>();
        map.put("processing", TransactionStatusEnum.PROCESSING);
        map.put("authorization_succeeded", TransactionStatusEnum.AUTHORIZATION_SUCCEEDED);
        map.put("authorization_declined", TransactionStatusEnum.AUTHORIZATION_DECLINED);
        map.put("authorization_failed", TransactionStatusEnum.AUTHORIZATION_FAILED);
        map.put("authorization_voided", TransactionStatusEnum.AUTHORIZATION_VOIDED);
        map.put("authorization_void_pending", TransactionStatusEnum.AUTHORIZATION_VOID_PENDING);
        map.put("capture_succeeded", TransactionStatusEnum.CAPTURE_SUCCEEDED);
        map.put("capture_pending", TransactionStatusEnum.CAPTURE_PENDING);
        map.put("buyer_approval_pending", TransactionStatusEnum.BUYER_APPROVAL_PENDING);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<TransactionStatus> {

        protected _Serializer() {
            super(TransactionStatus.class);
        }

        @Override
        public void serialize(TransactionStatus value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<TransactionStatus> {

        protected _Deserializer() {
            super(TransactionStatus.class);
        }

        @Override
        public TransactionStatus deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return TransactionStatus.of(v);
        }
    }
    
    public enum TransactionStatusEnum {

        PROCESSING("processing"),
        AUTHORIZATION_SUCCEEDED("authorization_succeeded"),
        AUTHORIZATION_DECLINED("authorization_declined"),
        AUTHORIZATION_FAILED("authorization_failed"),
        AUTHORIZATION_VOIDED("authorization_voided"),
        AUTHORIZATION_VOID_PENDING("authorization_void_pending"),
        CAPTURE_SUCCEEDED("capture_succeeded"),
        CAPTURE_PENDING("capture_pending"),
        BUYER_APPROVAL_PENDING("buyer_approval_pending"),;

        private final String value;

        private TransactionStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

