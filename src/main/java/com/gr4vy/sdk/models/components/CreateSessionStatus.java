/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class CreateSessionStatus {

    public static final CreateSessionStatus SUCCEEDED = new CreateSessionStatus("succeeded");
    public static final CreateSessionStatus FAILED = new CreateSessionStatus("failed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, CreateSessionStatus> values = createValuesMap();
    private static final Map<String, CreateSessionStatusEnum> enums = createEnumsMap();

    private final String value;

    private CreateSessionStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a CreateSessionStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as CreateSessionStatus
     */ 
    @JsonCreator
    public static CreateSessionStatus of(String value) {
        synchronized (CreateSessionStatus.class) {
            return values.computeIfAbsent(value, v -> new CreateSessionStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<CreateSessionStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        CreateSessionStatus other = (CreateSessionStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "CreateSessionStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static CreateSessionStatus[] values() {
        synchronized (CreateSessionStatus.class) {
            return values.values().toArray(new CreateSessionStatus[] {});
        }
    }

    private static final Map<String, CreateSessionStatus> createValuesMap() {
        Map<String, CreateSessionStatus> map = new LinkedHashMap<>();
        map.put("succeeded", SUCCEEDED);
        map.put("failed", FAILED);
        return map;
    }

    private static final Map<String, CreateSessionStatusEnum> createEnumsMap() {
        Map<String, CreateSessionStatusEnum> map = new HashMap<>();
        map.put("succeeded", CreateSessionStatusEnum.SUCCEEDED);
        map.put("failed", CreateSessionStatusEnum.FAILED);
        return map;
    }
    
    
    public enum CreateSessionStatusEnum {

        SUCCEEDED("succeeded"),
        FAILED("failed"),;

        private final String value;

        private CreateSessionStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

