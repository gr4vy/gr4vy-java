/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class TransactionIntent {

    public static final TransactionIntent AUTHORIZE = new TransactionIntent("authorize");
    public static final TransactionIntent CAPTURE = new TransactionIntent("capture");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TransactionIntent> values = createValuesMap();
    private static final Map<String, TransactionIntentEnum> enums = createEnumsMap();

    private final String value;

    private TransactionIntent(String value) {
        this.value = value;
    }

    /**
     * Returns a TransactionIntent with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TransactionIntent
     */ 
    @JsonCreator
    public static TransactionIntent of(String value) {
        synchronized (TransactionIntent.class) {
            return values.computeIfAbsent(value, v -> new TransactionIntent(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TransactionIntentEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TransactionIntent other = (TransactionIntent) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TransactionIntent [value=" + value + "]";
    }

    // return an array just like an enum
    public static TransactionIntent[] values() {
        synchronized (TransactionIntent.class) {
            return values.values().toArray(new TransactionIntent[] {});
        }
    }

    private static final Map<String, TransactionIntent> createValuesMap() {
        Map<String, TransactionIntent> map = new LinkedHashMap<>();
        map.put("authorize", AUTHORIZE);
        map.put("capture", CAPTURE);
        return map;
    }

    private static final Map<String, TransactionIntentEnum> createEnumsMap() {
        Map<String, TransactionIntentEnum> map = new HashMap<>();
        map.put("authorize", TransactionIntentEnum.AUTHORIZE);
        map.put("capture", TransactionIntentEnum.CAPTURE);
        return map;
    }
    
    
    public enum TransactionIntentEnum {

        AUTHORIZE("authorize"),
        CAPTURE("capture"),;

        private final String value;

        private TransactionIntentEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

