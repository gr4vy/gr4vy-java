/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gr4vy.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * Interval
 * 
 * <p>The cadence unit for the subscription plan.
 */
public class Interval {

    public static final Interval DAY = new Interval("DAY");
    public static final Interval WEEK = new Interval("WEEK");
    public static final Interval MONTH = new Interval("MONTH");
    public static final Interval YEAR = new Interval("YEAR");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Interval> values = createValuesMap();
    private static final Map<String, IntervalEnum> enums = createEnumsMap();

    private final String value;

    private Interval(String value) {
        this.value = value;
    }

    /**
     * Returns a Interval with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Interval
     */ 
    @JsonCreator
    public static Interval of(String value) {
        synchronized (Interval.class) {
            return values.computeIfAbsent(value, v -> new Interval(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<IntervalEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Interval other = (Interval) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Interval [value=" + value + "]";
    }

    // return an array just like an enum
    public static Interval[] values() {
        synchronized (Interval.class) {
            return values.values().toArray(new Interval[] {});
        }
    }

    private static final Map<String, Interval> createValuesMap() {
        Map<String, Interval> map = new LinkedHashMap<>();
        map.put("DAY", DAY);
        map.put("WEEK", WEEK);
        map.put("MONTH", MONTH);
        map.put("YEAR", YEAR);
        return map;
    }

    private static final Map<String, IntervalEnum> createEnumsMap() {
        Map<String, IntervalEnum> map = new HashMap<>();
        map.put("DAY", IntervalEnum.DAY);
        map.put("WEEK", IntervalEnum.WEEK);
        map.put("MONTH", IntervalEnum.MONTH);
        map.put("YEAR", IntervalEnum.YEAR);
        return map;
    }
    
    
    public enum IntervalEnum {

        DAY("DAY"),
        WEEK("WEEK"),
        MONTH("MONTH"),
        YEAR("YEAR"),;

        private final String value;

        private IntervalEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

