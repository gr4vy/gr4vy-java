# CreateSessionStatus

## Example Usage

```java
import com.gr4vy.sdk.models.components.CreateSessionStatus;

CreateSessionStatus value = CreateSessionStatus.SUCCEEDED;

// Open enum: use .of() to create instances from custom string values
CreateSessionStatus custom = CreateSessionStatus.of("custom_value");
```


## Values

| Name        | Value       |
| ----------- | ----------- |
| `SUCCEEDED` | succeeded   |
| `FAILED`    | failed      |