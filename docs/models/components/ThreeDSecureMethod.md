# ThreeDSecureMethod

## Example Usage

```java
import com.gr4vy.sdk.models.components.ThreeDSecureMethod;

ThreeDSecureMethod value = ThreeDSecureMethod.CHALLENGE;

// Open enum: use .of() to create instances from custom string values
ThreeDSecureMethod custom = ThreeDSecureMethod.of("custom_value");
```


## Values

| Name           | Value          |
| -------------- | -------------- |
| `CHALLENGE`    | challenge      |
| `FRICTIONLESS` | frictionless   |