# AgeGroup

## Example Usage

```java
import com.gr4vy.sdk.models.components.AgeGroup;

AgeGroup value = AgeGroup.ADULT;

// Open enum: use .of() to create instances from custom string values
AgeGroup custom = AgeGroup.of("custom_value");
```


## Values

| Name     | Value    |
| -------- | -------- |
| `ADULT`  | adult    |
| `INFANT` | infant   |