# ApprovalTarget

## Example Usage

```java
import com.gr4vy.sdk.models.components.ApprovalTarget;

ApprovalTarget value = ApprovalTarget.NEW_WINDOW;

// Open enum: use .of() to create instances from custom string values
ApprovalTarget custom = ApprovalTarget.of("custom_value");
```


## Values

| Name         | Value        |
| ------------ | ------------ |
| `NEW_WINDOW` | new_window   |
| `ANY`        | any          |